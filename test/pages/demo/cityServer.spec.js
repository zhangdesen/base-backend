import Vue from 'vue'
import cityService from '@/pages/demo/cityService/index.vue'

describe('cityService', () => {
  it('异步请求', () => {
    // 获取mount中的组件实例
    // const vmComponent = new Vue(cityService).$mount()
    // const { data } = vmComponent.getAreaList()
    // expect(type data).toBe('object')
  })
})
